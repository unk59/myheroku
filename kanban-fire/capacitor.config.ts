import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'kanban-fire',
  webDir: 'dist/kanban-fire',
  bundledWebRuntime: false
};

export default config;
